# Muestras para tests

- **`api-rama-judicial/`** — Respuestas JSON de la API de consulta procesos (anonimizadas: sin nombres reales de partes si se usan en CI).

No subir datos personales reales sin anonimizar.
